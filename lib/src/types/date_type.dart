/// The unit of the timestamp.
enum TimestampUnit {
  /// The unit of the timestamp is seconds.
  seconds,

  /// The unit of the timestamp is milliseconds.
  milliseconds,

  /// The unit of the timestamp is microseconds.
  microseconds,
}
